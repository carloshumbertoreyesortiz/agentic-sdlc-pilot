{
  "mcpServers": {
    "filesystem": {
      "command": "mcp-server-filesystem",
      "args": ["."]
    }
  },
  "_disabled_until_pat": {
    "github": {
      "note": "Enable once the fine-grained PAT exists (US-020/US-026): move under mcpServers and set GITHUB_PERSONAL_ACCESS_TOKEN from GH_AGENT_TOKEN.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "${GH_AGENT_TOKEN}" }
    }
  }
}
